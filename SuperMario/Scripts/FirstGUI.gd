extends Node

# class member variables go here, for example:
# var a = 2
# var b = "textvar"

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass
	#var name = get_node("/root/GlobalSwitch").getPlayerName()
	#get_node("RichTextLabel").add_text(str("Player name is : ", name))
	#var powerLevel = Globals.get("MAX_POWER_LEVEL")
	#get_node("Label").set_text(str("MAX_POWER_LEVEL is : ", powerLevel))